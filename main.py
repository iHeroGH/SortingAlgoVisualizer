from random import random
from Sorter import Sorter
from Render import Render


# l = Sorter.generate_random_list(5, 0, 10)
# s = Sorter(l)

# print(l, "\n")
# gen = s.bubble_sort(l)

# print(list(gen))

r = Render(algo = "selection")